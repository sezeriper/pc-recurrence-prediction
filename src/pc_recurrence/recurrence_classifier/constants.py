from pathlib import Path

DEFAULT_OUTPUT_ROOT = Path("outputs/recurrence_classification")
DEFAULT_PREDICTION_OUTPUT_ROOT = Path("outputs/recurrence_predictions")
TEST_FRACTION = 0.2
DECISION_THRESHOLD = 0.5
